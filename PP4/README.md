# 🧩 Práctica #4 - Árbol Binario con Arreglo Fijo

**Nombre:** Joshua Mitchell  
**Carné:** [FH22012006]  
**Curso:** SC-504 Lenguajes de Base de Datos (Java SE 21)  
  

---

## 🎯 Objetivo
Implementar una estructura de **árbol binario ordenado (Binary Search Tree)** utilizando un **arreglo de tamaño fijo (`Integer[]`)**.  
El programa debe permitir insertar valores de forma ordenada y recorrer el árbol en los tres órdenes:  
- **InOrder (izquierda → raíz → derecha)**  
- **PreOrder (raíz → izquierda → derecha)**  
- **PostOrder (izquierda → derecha → raíz)**  

Todo esto **sin usar recursividad**, únicamente con estructuras iterativas como bucles y pilas (`Stack`).

---

## ⚙️ Especificaciones Técnicas
- Lenguaje: **Java SE 21 / JDK 21 (LTS)**
- IDE utilizado: **Visual Studio Code** con extensión de Java.
- Clase utilizada: `BinTreeIntArray`
- Métodos actualizados:
  - `insert(int value)`
  - `preOrderTraversal()`
  - `postOrderTraversal()`
- Se respetó todo lo impreso en consola y no se modificó el `main`.

---

## 🧱 Estructura del Proyecto

